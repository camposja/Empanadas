<% content_for :title, "Nuevo contacto" %>

<div class="max-w-2xl">
  <div class="mb-6">
    <nav class="mb-1 text-sm text-gray-500" aria-label="Breadcrumb">
      <%= link_to "Contactos", admin_contacts_path, class: "transition-colors hover:text-primary-600" %>
      <span class="mx-1.5" aria-hidden="true">›</span>
      <span class="text-gray-900">Nuevo contacto</span>
    </nav>
    <h1 class="text-2xl font-bold text-gray-900">Nuevo contacto</h1>
  </div>

  <%= render "form", contact: @contact, cancel_path: admin_contacts_path %>
</div>
